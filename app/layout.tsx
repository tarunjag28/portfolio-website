import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Tarun Jagadish | Data Scientist & ML Engineer',
  description: 'Portfolio of Tarun Jagadish - MS in Applied Data Science at USC, specializing in Computer Vision, Machine Learning, and Data Analytics',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
